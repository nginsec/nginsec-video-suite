# nginsec Video Cloud Suite - Web Edition
# Install with: pip install -r requirements.txt
# FFmpeg must also be installed and on PATH for 1080p+ merged downloads and MP3 extraction.

flask>=3.0.0
flask-socketio>=5.3.6
python-socketio>=5.11.0
python-engineio>=4.9.0
yt-dlp>=2024.1.0
requests>=2.31.0
rookiepy>=0.5.0

# NOTE: Flask-SocketIO threading mode is used (no eventlet/gevent required).
# For higher concurrency under Ngrok or production load, optionally add:
#   eventlet>=0.35.1
# and change async_mode='threading' → 'eventlet' in app.py.
